const colorsString: string = "red,green,blue,yellow";

// Split string into array, using comma as separator
const colorsArray: string[] = colorsString.split(",");
console.log(colorsArray); // Output: ["red", "green", "blue", "yellow"]